name: Automated Inversion Engine Run

on:
  schedule:
    - cron: '0 */6 * * *' # Still auto-runs on schedule as standard backup
  workflow_dispatch: # Armed to receive custom parameters from your live web dashboard
    inputs:
      topic:
        description: 'Selected System Vector'
        required: true
        default: 'Gross Domestic Product (GDP)'
      format:
        description: 'Target Publication Format'
        required: true
        default: 'LinkedIn Professional Article Format'

permissions:
  contents: write

jobs:
  execute-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v4

      - name: Setup Runtime Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies
        run: pip install requests beautifulsoup4 google-generativeai

      - name: Run Dynamic Core Inversion Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # Pass the interactive dropdown choices straight to the environment variables
          INPUT_TOPIC: ${{ github.event.inputs.topic || 'Gross Domestic Product (GDP)' }}
          INPUT_FORMAT: ${{ github.event.inputs.format || 'LinkedIn Professional Article Format' }}
        run: python scripts/autonomous_engine.py

      - name: Commit Generated Drafts to Staged Folder
        run: |
          git config --global user.name "humanos-bot"
          git config --global user.email "bot@humanos.network"
          mkdir -p staged_outputs
          git add staged_outputs/
          git diff --quiet && git diff --staged --quiet || (git commit -m "ledger: save adaptive custom inversion report" && git push)
